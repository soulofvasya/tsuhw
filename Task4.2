#include <stdio.h>

int main()
{
        int x = 32;
        char y = '!';
        while (x < 127) {
                x += 1;
                y += 1;
                printf("%d | %c\n", x, y);
        }
}
